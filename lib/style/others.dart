import 'package:flutter/material.dart';

final kShadow = BoxShadow(
  color: Colors.black.withOpacity(0.16),
  spreadRadius: 3,
  blurRadius: 7,
  offset: const Offset(0, 6), // changes position of shadow
);

const kBorderRadius = 10.0;
const kDrawerRadius = 100.0;